# Backend ki origin. Dev mein default (http://localhost:8081) chalta hai, isliye local
# ke liye is file ki zarurat nahi.
#
# Isse badlo to backend ka FRONTEND_ORIGIN bhi badalna padega — CORS aur preview ke CSP
# frame-ancestors dono usi property se aate hain
VITE_API_BASE_URL=http://localhost:8081
